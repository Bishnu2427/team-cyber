"""
Recon Agent — Asset discovery, service detection, fingerprinting, tech stack analysis.

Tools (Phase 2): Nmap, WhatWeb, Amass, Subfinder, FFUF, Nuclei
Status: Phase 1 stub — architecture in place, active scanning in Phase 2.
"""
from typing import List


def run_recon(project_path: str, tech_stack: List[str]) -> List[dict]:
    # Phase 2: run nmap, whatweb, amass against live target
    # Phase 2: parse output → structured findings
    print("[recon-agent] Phase 2 — passive recon only in Phase 1")
    return []
