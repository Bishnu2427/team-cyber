"""
Web Security Agent — SQL Injection, XSS, CSRF, SSRF, Auth issues.

Tools (Phase 2): SQLMap, XSStrike, Nuclei, FFUF, Burp integrations
Status: Phase 1 stub.
"""
from typing import List


def run_web_analysis(project_path: str, tech_stack: List[str]) -> List[dict]:
    # Phase 2: run sqlmap, xsstrike, nuclei web templates
    print("[web-agent] Phase 2 — dynamic web testing not active in Phase 1")
    return []
